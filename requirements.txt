# ✂️ AnimeCut Serverless v12 ULTIMATE Requirements
# Este arquivo prioriza estabilidade e velocidade de build

# ==================== CORE ====================
runpod>=1.6.0
requests>=2.31.0
tqdm>=4.66.4
colorama

# ==================== NUMPY SHIELD (CRÍTICO) ====================
# Deve ser < 2.0 para compatibilidade com PyTorch e OpenCV atuais
numpy==1.26.4

# ==================== PROCESSAMENTO DE VÍDEO ====================
# MoviePy v2 (Dev) para melhor performance e menos bugs com numpy novo
moviepy==1.0.3
imageio>=2.34.1
imageio-ffmpeg>=0.5.1
# OpenCV Headless para ambiente servidor
opencv-python-headless>=4.9.0.80

# ==================== IA & VISÃO (STACK TURBO) ====================
# YOLO para Detecção de Rostos e Smart Crop
ultralytics

# Limpeza de Áudio Profissional
deepfilternet

# Transcrição Acelerada (Flash Attention requer GPU, aqui usamos fallback CPU-safe)
# insanely-fast-whisper instala transformers e torch, cuidaremos para usar versões compatíveis
insanely-fast-whisper
transformers>=4.40.0
accelerate>=0.30.0
optimum
scipy

# ==================== OUTROS ====================
boto3>=1.34.0
botocore>=1.34.0
Pillow>=10.3.0
soundfile>=0.12.1
decorator<5.0
librosa

# Utilitários de compilação
Cython<3
